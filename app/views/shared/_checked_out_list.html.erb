<%# app/views/shared/_list.html.erb %>
<div id="filterrific_results">

 <!-- <div>
    <%# page_entries_info items # provided by will_paginate %>
  </div> -->

  <table>
    <tr>
      <th>Name</th>
      <% if params[:id] %>
        <th>Checked Out To Member Qty</th>
      <%else%>
         <th>Checked Out Qty</th>
      <%end%>
      <th>Category</th>
      <% if params[:id] %>
        <th>Action</th>
      <%end%>

    </tr>
    <% items.each do |item| %>
      <tr>
        <td><%= link_to(item.name, item_path(item)) %></td>
        <% if params[:id] %>
          <td><%= item.checked_out_to_member_quantity(params[:id]) %></td>
        <%else%>
          <td><%= item.checked_out_quantity %></td>
        <%end%>
        <td><%= item.category.name %></td>
        <% if params[:id] %>
          <td>check in</td>
        <%end%>
      </tr>
    <% end %>
  </table>
</div>

<%# will_paginate items # provided by will_paginate %>